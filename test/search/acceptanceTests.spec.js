// import the `mongoose` helper utilities
let utils = require('../utils');
import chai from 'chai';
let should = chai.should();
let util = require('util');

describe('SEARCH: ACCEPTANCE TESTS ***********************************', () => {

  describe('US: Search Organizations', () => {
    it("should redirect to a 'list hero search results page' when I click the 'pesquisar indivíduos' button, enter search terms in the search bar and and click in the 'pesquisar' button");
  });

  describe('US: Search Heroes', () => {
    it("should redirect to a 'list organization search results page' when I click the 'pesquisar organizações' button, enter search terms in the search bar and and click in the 'pesquisar' button");
  });
});
