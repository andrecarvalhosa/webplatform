// import the `mongoose` helper utilities
let utils = require('../utils');
import chai from 'chai';
let should = chai.should();
let util = require('util');

describe('BENEFITS: ACCEPTANCE TESTS *********************************', () => {

  describe('US: Access info on hiring benefits', () => {
    it("should redirect to a 'benefits page' when I click in the button 'benefícios de contratar pessoas com incapacidades'");
    });

  describe('US: Access to Manual', () => {
    it("should have an 'welcoming guide'");
  });
});
